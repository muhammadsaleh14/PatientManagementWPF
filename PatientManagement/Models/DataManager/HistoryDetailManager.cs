namespace PatientManagement.Models.DataManager
{
    public static class HistoryDetailManager
    {
        //public static HistoryDetail AddOrReturnHistoryDetail(string newDetailText, PatientContext? db)
        //{
        //    db ??= new PatientContext();

        //    var existingDetail = db.HistoryDetails.FirstOrDefault(d => d.Detail == newDetailText);

        //    // If it doesn't exist, create a new one
        //    if (existingDetail == null)
        //    {
        //        existingDetail = new HistoryDetail(null, newDetailText);
        //        db.HistoryDetails.Add(existingDetail);
        //        db.SaveChanges();
        //    }

        //    return existingDetail;
        //}
    }
}
