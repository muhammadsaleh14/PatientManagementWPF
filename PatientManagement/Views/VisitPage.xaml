<UserControl x:Class="PatientManagement.Views.VisitPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PatientManagement.Views"
        xmlns:components="clr-namespace:PatientManagement.Views.Components"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800"
>
    <Grid HorizontalAlignment="Stretch" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Content="Name" Grid.Row="0" Grid.Column="0"/>
                <TextBlock Text="{Binding Visit.Patient.Name}"  Grid.Row="1" Grid.Column="1"/>

                <Label Content="Age" Grid.Row="0" Grid.Column="2"/>
                <TextBlock Text="{Binding Visit.Patient.Age}" Grid.Row="1" Grid.Column="3"/>

                <Label Content="Gender" Grid.Row="0" Grid.Column="4"/>
                <TextBlock Text="{Binding Visit.Patient.Gender}" Grid.Row="1" Grid.Column="5"/>
            </Grid>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <components:HistoryControl Grid.Column="0" />
            
            <StackPanel Grid.Column="1">
                <components:Prescriptions
                    DataContext="{Binding PrescriptionsViewModel}"/>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
